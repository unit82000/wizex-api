<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wisenc.wizex.mapper.wizexapi.WizexApiExamMapper">

	<select id="list" resultType="com.wisenc.wizex.api.exam.vo.ProductVO">
		SELECT
		    *
		FROM
		    PRODUCT
		WHERE
		    ROWNUM <![CDATA[ < ]]> 10
	</select>
</mapper>